# Movie-Review-Analysis
This project analyzes movie review sentiment using the IMDB dataset. A SimpleRNN model processes tokenized and padded reviews to classify them as positive or negative. After training for high accuracy, the model is deployed via Streamlit, enabling users to input reviews and get real-time sentiment predictions, showcasing AI in text analysis.
